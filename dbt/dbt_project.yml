name: 'laplaptech'
version: '1.0.0'
config-version: 2

profile: 'laplaptech'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  laplaptech:
    bronze:
      +materialized: table
    silver:
      +materialized: table
    gold:
      +materialized: table
      hardware:
        +schema: hardware
      marketing:
        +schema: marketing

